use "atlas-scripts-sml/ffi/AtlasFFI.sml";

(*
  File: atlas-scripts-sml/tits_centralizer.sml

  Purpose
  - Translation scaffold for `atlas-scripts/tits_centralizer.at`.
  - See the `.at` file for intended Tits group / centralizer computations.

  Status
  - Not yet ported: this file currently defines only a placeholder structure.
*)

structure Tits_centralizer = struct
  fun TODO (_: string) : 'a =
    raise Fail "Tits_centralizer: not yet ported"
end

