use "atlas-scripts-sml/ffi/AtlasFFI.sml";

(*
  File: atlas-scripts-sml/weak_packets.sml

  Purpose
  - Translation scaffold for `atlas-scripts/weak_packets.at`.
  - See the `.at` file for intended weak packet computations.

  Status
  - Not yet ported: this file currently defines only a placeholder structure.
*)

structure Weak_packets = struct
  fun TODO (_: string) : 'a =
    raise Fail "Weak_packets: not yet ported"
end

