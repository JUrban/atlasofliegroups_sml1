use "atlas-scripts-sml/ffi/AtlasFFI.sml";

(*
  File: atlas-scripts-sml/subgroups.sml

  Purpose
  - Translation scaffold for `atlas-scripts/subgroups.at`.
  - See the `.at` file for intended subgroup enumeration utilities.

  Status
  - Not yet ported: this file currently defines only a placeholder structure.
*)

structure Subgroups = struct
  fun TODO (_: string) : 'a =
    raise Fail "Subgroups: not yet ported"
end

