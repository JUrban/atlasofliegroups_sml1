use "atlas-scripts-sml/ffi/AtlasFFI.sml";

(*
  File: atlas-scripts-sml/2r21s.sml

  Purpose
  - Placeholder SML translation of `atlas-scripts/2r21s.at`.
  - The `.at` script is another small “extended parameter / quotient root datum”
    example similar in spirit to `2i12.at`/`2i12s.at`.

  Status
  - Not yet implemented in SML; see the notes in `atlas-scripts-sml/2i12.sml`.
*)

structure Script2r21s = struct
  fun run () : unit =
    raise Fail "2r21s: not yet ported (depends on extended/Hecke infrastructure)"
end

