use "atlas-scripts-sml/FPP_vertices_fold.sml";

(*
  File: atlas-scripts-sml/FPP_vertices.sml

  Purpose
  - Compatibility wrapper matching the `.at` filename `FPP_vertices.at`.
  - The implementation lives in `atlas-scripts-sml/FPP_vertices_fold.sml`.
*)

structure FPP_vertices = FPP_vertices_fold;

