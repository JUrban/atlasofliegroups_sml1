use "atlas-scripts-sml/ffi/AtlasFFI.sml";

(*
  File: atlas-scripts-sml/coherent_characters.sml

  Purpose
  - Translation scaffold for `atlas-scripts/coherent_characters.at`.
  - See the `.at` file for intended functionality and interfaces.

  Status
  - Not yet ported: this file currently defines only a placeholder structure.
*)

structure Coherent_characters = struct
  fun TODO (_: string) : 'a =
    raise Fail "Coherent_characters: not yet ported"
end

