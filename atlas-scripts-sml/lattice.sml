use "atlas-scripts-sml/LatticeAT.sml";

(*
  File: atlas-scripts-sml/lattice.sml

  Purpose
  - Compatibility wrapper matching the `.at` filename `lattice.at`.
  - The `.at`-style helper layer lives in `atlas-scripts-sml/LatticeAT.sml` as
    `structure LatticeAT` (distinct from `atlas-scripts-sml/Lattice.sml` which
    provides the low-level matrix/ratvec operations).
*)

