use "atlas-scripts-sml/ffi/AtlasFFI.sml";

(*
  File: atlas-scripts-sml/cohIndUnip.sml

  Purpose
  - Translation scaffold for `atlas-scripts/cohIndUnip.at`.
  - See the `.at` file for intended functionality and interfaces.

  Status
  - Not yet ported: this file currently defines only a placeholder structure.
*)

structure CohIndUnip = struct
  fun TODO (_: string) : 'a =
    raise Fail "CohIndUnip: not yet ported"
end

